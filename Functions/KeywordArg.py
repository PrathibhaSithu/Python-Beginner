def increment(number, by):
    return number + by


result = increment(2, 1)

print(result)

#  or we can use this

print(increment(9, by=1))  # This will also print the result directly

# by=1 is a keyword argument

# keyword arguments allow you to specify arguments by their parameter name
